  project_id: ha65125-cea80
  client_id: !secret assistant_client_id
  access_token: !secret assistant_access_token
  #agent_user_id: [a string to identify user]
  api_key: !secret assistant_api_key
  exposed_domains:
    - switch
    - light
    - group
